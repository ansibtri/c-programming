1. use gcc -Wall -save-temps helloworld.c -o anish
>>>here gcc is a compiler and rest is called gcc compiler's flag use to see three files i.e .i, .o and .s.
>>>.i files include the required functions from headers and removes the comments.
>>>.s files includes the assembly code which is converted from .c file
>>>.o is a binary file
>>>.exe is a final files that is gonna execute.
>>>%d is format identifier where d represents decimal integer.
>>> here 'anish' is the custom filename given by user.